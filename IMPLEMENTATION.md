# 公管学院教师导航实现方案

## 目标与设计

基于公共管理学院官网，参考 [计算机学院教师导航](https://github.com/yaowenhu-pm/uestc-scse-faculty-explorer)，构建可公开访问、可复现更新的静态网站。

遵循已确认的 [Figma稿](https://www.figma.com/design/byIMpOiumrSh1zPMdhvCfz?node-id=2-2)：白底、深蓝文字、灰绿强调色；学校与学院名称、搜索筛选、学系导航、教师卡片构成首页。评级位于卡片右上角，完整说明放在详情和评级说明页。图稿中的评级字母只示意布局，正式值由官网证据生成。

## 页面与交互

| 模块 | 实现内容 |
| --- | --- |
| 首页 | 教师目录、人数、关键词搜索、职称和评级筛选、学系导航、排序、分批加载 |
| 卡片 | 姓名、职称、学系、精简研究方向和评级；支持点击及键盘打开 |
| 详情 | 研究方向、五项分数、官网摘录、资料局限、官网链接、复制教师深链 |
| 评级说明 | 分项档位、等级阈值、S级门槛、来源及缺失信息解释 |
| 空态与失败态 | 无匹配可清空筛选；加载失败可重试；未知地址可返回首页 |
| 手机 | 筛选换行、学系横向滚动、单列卡片、可滚动详情 |

筛选与教师ID保存在URL中，刷新或分享后恢复。默认按S、A、B、C、D、E等级顺序排列，同级按证据分数降序，同分按官网目录顺序；也可切换姓名或官网目录排序。键盘焦点可见，结果数量通过状态区域提示，详情支持Escape关闭。

## 技术架构

- HTML、CSS、原生JavaScript，复用参考仓库的静态架构，无前端运行时依赖、后端、数据库或密钥。
- `app.js` 负责筛选、排序与展示；文本进入HTML前转义，外链验证协议。
- `data/faculty.source.json` 为脱敏公开证据输入，可在净检出中复现。
- `scripts/build-public-data.mjs` 生成教师公开数据、统计及版本。
- `scripts/validate-public-data.mjs` 校验名录完整性、字段白名单、出处、评分及敏感信息。
- 发布脚本只打包明确允许的网站文件；浏览器仅读取生成后的公开JSON。

## 数据与评级

来源为[按姓氏字母目录](https://spa.uestc.edu.cn/szdw/jsml/xsszm.htm)、[学系目录](https://spa.uestc.edu.cn/szdw/jsml/xsyq.htm)与各教师详情页。初始快照为2026年9月20日，69位教师，涵盖6个学系及特聘讲席教授目录。

评级沿用参考项目的“官网证据强度”含义：外部认可30分、项目证据20分、成果证据20分、学术任职20分、培养教学10分。每项取官网明确支持的最高档位，非零项保留原文及出处；S级另需明确的国家级个人认可，或本人主持国家社科基金重大项目、担任该重大项目首席专家的证据。具体档位以 `methodology.html` 和版本化生成器为准，不根据期望分布反推分数。

公开仓库不保存邮箱、电话、办公室、完整履历、原始HTML、浏览器状态或运行凭据。完整采集材料在仓库外保存。缺失证据不代表没有对应经历，历史职务不自动解释为当前状态；项目为独立信息索引。

## 更新与发布

1. 在仓库外采集并核对官网变化，整理公开证据。
2. 更新 `faculty.source.json`；目录人数变动时同步修改完整性校验和测试。
3. `npm run check` 验证数据、评分、可复现构建与行为测试。
4. 本地检查桌面/手机、联合筛选、详情及URL恢复。
5. GitHub PR执行校验，`main`通过后由Actions部署GitHub Pages。
6. 在线核对最新提交、人数、关键交互及部署结果。

CI不在线抓取学院官网，也不依赖私人数据或Figma连接。回滚通过恢复已验证的Git提交并重新部署完成。

## 验收标准

- 69位教师无重复、无遗漏，资料与证据可追溯。
- 分数由公开输入复算，代码和说明一致，无示例分数混入。
- 桌面符合设计，手机无整页横向溢出。
- 搜索、联合筛选、排序、加载更多、清空、详情、分享及刷新恢复可用。
- 键盘可操作，详情关闭后焦点返回合理位置。
- 空态、加载失败和404均可恢复。
- 净检出可检查，Pages产物不包含源材料或运行日志。
- GitHub仓库和生产网址可访问，Actions部署成功。
