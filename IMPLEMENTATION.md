# 公管学院教师导航实现方案

## 目标与设计

基于学院官网，参考 [计算机学院教师导航](https://github.com/yaowenhu-pm/uestc-scse-faculty-explorer)，构建可公开访问、可复现更新的静态目录。

遵循已确认的 [Figma稿](https://www.figma.com/design/byIMpOiumrSh1zPMdhvCfz?node-id=2-2)：白底、深蓝文字、灰绿强调色；学校与学院名称、搜索筛选、学系导航、教师卡片构成首页。卡片只显示姓名、职称、学系、精简研究方向及分级。图稿中的字母仅示意布局，正式值由官网证据和版本化规则生成。

## 页面与交互

| 模块 | 实现内容 |
| --- | --- |
| 首页 | 目录与人数、关键词搜索、职称和分级筛选、学系导航、排序；全部69人或全部匹配结果一次显示，自然滚动，无加载更多 |
| 卡片 | S至E均显示同档细分字母（如S+、A、B-、E+），只有资料不足“—”不细分；点击或键盘打开详情 |
| 已分级详情 | 一句分级理由、资料摘要、总分、可展开的分项与细则、分组原文依据 |
| 资料不足详情 | 说明暂不分级与缺失信息，不显示总分或分项数值 |
| 来源与分享 | 教师官网、逐条摘录出处、可选Crossref署名核对、复制教师深链 |
| 方法说明 | 简洁说明页；完整规则单独保存在 `RATING_PLAN.md` |
| 空态与失败态 | 无匹配可清空筛选；加载失败可重试；未知地址可返回首页 |
| 手机与键盘 | 搜索换行、学系横向滚动、单列卡片、可滚动详情和可见焦点 |

筛选条件与教师ID保存在URL中，刷新、分享或历史导航后恢复。默认按基础档S、A、B、C、D、E、U排列，同档按分数降序，同分按官网目录顺序；显示顺序对应S+、S、S-、A+、A、A-、B+、B、B-、C+、C、C-、D+、D、D-、E+、E、E-、U。可切换姓名或官网目录排序，资料不足可单独筛选。结果数量通过状态区域提示，详情支持Escape关闭，展开说明使用原生 `details`。

## 技术与数据流

使用HTML、CSS及原生JavaScript，无前端运行时依赖、后端、数据库或密钥。

1. 在仓库外采集并保留原始页面，逐人复核项目、代表作、本人角色、年份与出处。
2. 将允许公开的字段和最小原文摘录写入 `data/faculty.source.json`。
3. 生成器校验输入，计算分项、资料资格、总分及基础档，再计算同档细分，同时生成解释与统计。
4. 浏览器仅读取生成后的公开教师数据与统计，完成筛选和展示。
5. 发布脚本仅复制明确列入白名单的网站文件，禁止整目录复制采集材料。

`app.js` 对文本进行HTML转义，外链限制为HTTP或HTTPS；既有公开键保持兼容。v3增加 `assessmentStatus`、`coverage`、`gradeReason`、`scoringNotes`。`coverage`是本次选择的证据量，不是生涯总量。U的公开总分为 `null`；保留的分项数值仅供计算审计，前端不显示为低分。

v3.2沿用既有两级字段，`evidenceGrade`保留基础档，`evidenceSubgrade`保存显示档位，S至E可带ASCII `+`或`-`，U保持原值。`statistics.json`继续保留 `gradeDistribution` 和 `subgradeDistribution`；每个基础档的细分人数之和应与原基础档统计相等，空子档可以为0。前端展示使用细分字段，U仍显示“—”；兼容旧数据时可回退到基础档。本轮不增加字段。

证据可附`authorVerification: { sourceUrl, doi, authorFullName, authorPosition }`，用于记录同一作品的Crossref署名补核。官网摘录与补核来源分开保留；前端在对应证据下显示作者全名、顺序和链接，仅核对英文缩写，不用于影响力计分。缺少补证时不推断缩写身份。前端读取教师及统计JSON时仍携带`?v=20260921-v3.2`，数据请求版本与前端资源缓存版本分别管理。

CSS与JS使用`?v=20260921-scroll`避免上线后加载旧前端；本次列表调整不改变数据或评分规则，规则版本仍为v3.2，生成数据与规则说明保持一致。

## 规则与来源

官网快照为2026-09-20，69位教师，涵盖6个学系及特聘讲席教授目录。规则审阅日期为2026-09-21，版本 `spa-research-profile-v3.2`。近期窗口为2021–2026年，只使用采集时已列出的成果。

五项上限为科研认可30、科研项目30、代表成果25、学术服务10、培养教学5。先通过资料门槛，再按S≥75及个人国家级认可／国家重大项目门槛、A≥60及项目≥18或成果≥15门槛、B≥45、C≥30、D≥15、其余E依次判定；资料不足优先标U。

随后在已经确定的S至E内细分：S-75–79、S80–84、S+85–100；A-60–64、A65–69、A+≥70；B-45–49、B50–54、B+≥55；C-30–34、C35–39、C+40–44；D-15–19、D20–24、D+25–29；E-0–4、E5–9、E+10–14。只有U不细分。S的全部细分都先要求符合原S门槛；75分及以上未过S但已满足A门槛时为A+，60分及以上未过A门槛时为B+，不能借“+”晋级。细分不改变原总分、基础档、S/A门槛或来源日期，固定阈值与人数无关，允许空子档，不按人数三等分。

项目区分本人角色、资助层级与子课题，只对有限数量的不同国家主持项目加分；最多5项代表作按完整引文、明确角色、近期和连续年份信号计分。缺少期刊、出版社或采纳机构的成果不计分。不按期刊等级、收录或影响因子评价个人，不强制等级比例。所有档位、公式和例外以 [RATING_PLAN.md](./RATING_PLAN.md)为准。

教育部相关意见与DORA仅启发分类、多类型代表作和避免期刊指标替代个人评价；不为本项目的权重、等级或排序背书。本项目是公开履历信号索引，不能替代实际成果阅读、同行评价或与教师沟通。

## 更新与发布流程

1. 先记录规则版本和资料日期，再整理输入；调整权重不冒充资料更新。
2. 全量复核名录，去重项目和代表作，核对本人归属及 `venue` 字段。
3. 执行 `npm run check` 检查数据、评分、复现性和行为；执行 `npm run build` 生成 `_site/`。
4. 检查桌面／手机、联合筛选、默认分级排序、U筛选、详情展开、来源和深链。
5. PR运行校验，`main`通过后由GitHub Actions部署GitHub Pages。
6. 在线核对最新提交、人数、数据版本、关键交互及部署状态。

CI不在线抓取学院官网，不依赖私人数据或Figma连接。公开仓库和发布包不保存邮箱、电话、办公室、照片、完整履历、原始HTML、浏览器状态或运行凭据。回滚通过恢复已验证的Git提交并重新部署完成。

## 验收标准

- 69位教师无重复、无遗漏；网页与规则审阅日期分别记录。
- 项目和成果逐条可追溯，缺少出处或本人角色时按规则处理，不编造记录。
- 资料资格、U优先、S/A门槛、每档与细分阈值、加分封顶与去重均有边界验证；A+/B+不绕过门槛晋级。
- v3.2扩展S/E细分前后原总分、分项、基础档及A至D细分完全一致；细分人数能回汇至基础档人数，空子档允许为0，只有U不细分。
- 同一输入与规则可复算，理由与得分一致，无图稿示例评级混入。
- U排序靠后，可筛选；详情没有总分或分项分值。
- 桌面符合批准设计，手机无整页横向溢出，键盘焦点可见。
- 搜索、联合筛选、排序、全量结果自然滚动、清空、详情、展开依据、分享及URL恢复可用。
- 空态、加载失败和404可恢复；外链有效，文本安全转义。
- 净检出可检查，发布白名单不包含采集档案或运行日志。
- 仓库与生产网址可访问，Actions部署成功；分布仅记录结果，不反向驱动规则。
